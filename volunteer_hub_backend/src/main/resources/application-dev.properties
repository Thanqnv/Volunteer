################################
# DATABASE CONFIG
################################
# Local PostgreSQL (Docker hoặc local)
spring.datasource.url=jdbc:postgresql://localhost:5432/volunteer_hub_db?rewriteBatchedStatements=true
spring.datasource.username=postgres
spring.datasource.password=sqlinjection
spring.datasource.driver-class-name=org.postgresql.Driver

################################
# JPA / HIBERNATE CONFIG
################################
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Hibernate JDBC Batch - optimize performance
spring.jpa.properties.hibernate.jdbc.batch_size=25
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

################################
# TOKEN / SECURITY TTL
################################
# Password reset token TTL (minutes)
password-reset.token.ttl-minutes=15

# Recovery code TTL (minutes) - Redis
recovery.code.ttl-minutes=10

################################
# REDIS CONFIG
################################
spring.data.redis.host=localhost
spring.data.redis.port=6379

################################
# EMAIL CONFIG (GMAIL SMTP)
################################
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=thaiviethoang2910@gmail.com
spring.mail.password=nrgy pkui jajj njjx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

################################
# LOGGING
################################
logging.level.vnu.uet.volunteer_hub.volunteer_hub_backend.service.EmailService=DEBUG
logging.level.vnu.uet.volunteer_hub.volunteer_hub_backend.api.AuthAPI=INFO
logging.level.org.springframework.mail=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

################################
# JWT CONFIG
################################
# Main JWT config
security.jwt.secret=change-this-dev-secret-to-a-long-random-string-at-least-32-characters
security.jwt.expiration-ms=86400000

# Backward compatibility (avoid Missing placeholder)
jwt.secret=${security.jwt.secret}
jwt.expiration-ms=${security.jwt.expiration-ms}

################################
# FRONTEND & OAUTH2
################################
app.frontend.url=http://localhost:3000
app.oauth2.success-redirect-url=http://localhost:3000/oauth/callback

# GOOGLE OAUTH2 CONFIG
spring.security.oauth2.client.registration.google.client-id=197145757314-7ubbh305cl484dtepicdsqsbt03h9quh.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=email,profile
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}

spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/v2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v3/userinfo
spring.security.oauth2.client.provider.google.user-name-attribute=sub
spring.security.oauth2.client.registration.google.prompt=select_account

################################
# ACTUATOR (DEV ONLY)
################################
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
